LIST('https://raw.githubusercontent.com/spectx/aoc2020/main/day6/input/input.txt')
| parse(pattern:"ARRAY{ARRAY{[a-z]:x}+:l EOL}*:arr (EOL | EOF)")
| select(c:size(@1), q:unnest(unnest(@1)))
| select(r:count(*))
| group(_raw_offset, q)
| select(answer1:count(*), answer2:count(c = r))

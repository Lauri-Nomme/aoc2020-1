LIST('https://raw.githubusercontent.com/spectx/aoc2020/main/day2/input/input.txt')
| PARSE(pattern:"INT:p1 '-' INT:p2 ' ' [a-z]:char ': ' DATA:password EOL")
| select(c1:charat(password, int(p1-1)), c2:charat(password, int(p2-1)))
| filter((c1 == char) != (c2 == char))
| select(answer:count(*))




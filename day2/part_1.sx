LIST('https://raw.githubusercontent.com/spectx/aoc2020/main/day2/input/input.txt')
| PARSE(pattern:"INT:min '-' INT:max ' ' [a-z]:char ': ' DATA:password EOL")
| select(symCnt:size(split(password, char))-1)
| filter(symCnt >= min AND symCnt <= max)
| select(answer:count(*))
